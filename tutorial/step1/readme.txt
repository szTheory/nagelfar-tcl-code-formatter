This example contains two source files, apa.tcl and bepa.tcl.

The Makefile runs them through Nagelfar in the straightforward way that you
would typically use when starting out with using Nagelfar.

In most real code, as well as in these examples, there will be false negatives.

In the following steps we will set up a flow that handles those and make it
possible to get a clean run.
